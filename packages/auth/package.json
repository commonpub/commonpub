{
  "name": "@commonpub/auth",
  "version": "0.4.2",
  "type": "module",
  "description": "Better Auth wrapper with AP actor SSO and role guards for CommonPub",
  "license": "AGPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "https://github.com/commonpub/commonpub",
    "directory": "packages/auth"
  },
  "keywords": ["commonpub", "activitypub", "federation", "typescript", "auth", "sso", "better-auth"],
  "publishConfig": { "access": "public" },
  "engines": { "node": ">=22" },
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist/", "!dist/__tests__/", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@commonpub/schema": "workspace:*",
    "@commonpub/config": "workspace:*",
    "@commonpub/protocol": "workspace:*",
    "better-auth": "^1.2.0",
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
