import type { ModuleMeta } from '../types';

const meta: ModuleMeta = {
  id: 'slider',
  name: 'Slider + Canvas',
  description: 'Continuous variable with live visual feedback',
  icon: 'fa-sliders',
  color: '#e04030',
  category: 'input',
  contentFields: {
    heading: true,
    body: true,
    insight: true,
    bridge: true,
    aside: true,
  },
};

export default meta;
