import type { ModuleMeta } from '../types';

const meta: ModuleMeta = {
  id: 'hero',
  name: 'Hero Section',
  description: 'Opening section with title, subtitle, and scroll hint',
  icon: 'fa-flag',
  color: '#8b8b8b',
  category: 'layout',
  contentFields: {},
};

export default meta;
